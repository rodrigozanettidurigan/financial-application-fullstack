server.port=${PORT:8080}

spring.application.name=financial-project
spring.jpa.database=MySQL
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/financialapi?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=America/Sao_Paulo}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:}

spring.jpa.show-sql=true

spring.jackson.deserialization.fail-on-unknown-properties=true
spring.jackson.date-format=yyyy-MM-dd

app.security.issuer=${APP_ISSUER:http://localhost:8080}
